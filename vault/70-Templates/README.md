# Templates

One note per target client theme/template — the block patterns that theme
prefers, so `clean_llm`/`blocks` can bias toward them without hard-gating on
them (see [[30-Decisions/0001-componentized-pipeline-and-vault-learning]]).
The web UI's template dropdown (phase 3) lists the profiles found here. Read
before a migration targeting a specific client site whose theme has known
preferences.
